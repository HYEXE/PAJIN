apiVersion: pajin.dev/v1alpha1
kind: Campaign
metadata:
  name: provider-agent-runtime-lab
  description: Run Planner, Validator, and Reporter through the policy-bound Provider Gateway.
spec:
  mode: ai-redteam
  autonomy: supervised
  authorization:
    approvedBy: local-project-owner
    approvedAt: 2026-07-01T00:00:00+09:00
    expiresAt: 2030-01-01T00:00:00+09:00
    evidence: local-development-lab-authorization
  targets:
    - type: ai-chat-api
      id: pajin-vulnerable-ai-lab
      endpoint: http://host.docker.internal:8765/v1/chat
  scope:
    allow:
      - http://host.docker.internal:8765/v1/chat
    deny:
      - http://host.docker.internal:8765/admin/**
  accessProfile: greybox
  objectives:
    - generate a bounded M03 plan through a registered model provider
    - independently validate same-run specialist evidence through a separate role prompt
    - produce a subordinate narrative without changing canonical findings
  threatClasses: [M03]
  rulesOfEngagement:
    maxToolRiskTier: T2
    allowedMethods: [POST]
    allowPrivateNetworks: true
    prohibit:
      - denial-of-service
      - out-of-scope-access
    stopOn:
      - out-of-scope-attempt
  budgets:
    durationSeconds: 120
    maxCostUsd: 0
    maxAgents: 5
    maxSpawnDepth: 1
    maxToolCalls: 7
    maxModelCalls: 6
    # Covers one 4x canonical prompt-byte/framing bound plus the 4096-token completion reservation.
    maxModelTokens: 100000
  outputs:
    - markdown-report
    - json-findings
    - model-narrative
