# PAJIN 에이전트 작업 지침

이 파일은 저장소 전체에 적용된다. 시스템, 개발자 및 사용자의 명시적 지시처럼 우선순위가
더 높은 지침은 항상 이 파일보다 우선한다.

## 권위와 영속적 작업 상태

장기 작업 상태를 대화 컨텍스트에만 의존하지 않는다. 작업 시작 시 다음 파일을 읽고 각
문서가 담당하는 권위가 달라졌을 때 갱신한다.

- `AGENTS.md`: 저장소 전체에 지속적으로 적용되는 작업 규칙
- `PLAN.md`: 현재 로드맵, 우선순위, 마일스톤 및 완료 기준
- `HANDOFF.md`: 다음 세션이 실행할 수 있는 현재 체크포인트
- `DECISIONS.md`: 결정 문서 탐색용 색인. 채택된 근거는 `docs/adr/`이 권위다.
- `KNOWN_ISSUES.md`: 재현된 미해결 제약과 환경 제한

동작의 권위는 코드와 테스트에 있다. 보안 경계와 근거의 권위는 버전형 계약과 채택된
ADR에 있다. 우선순위는 `PLAN.md`, 최신 기록 체크포인트는 `HANDOFF.md`가 담당하되, 작업
재개 시 Git과 파일시스템을 통해 반드시 검증한다. 기존 Notion 로드맵은 역사적
스냅샷이며 더 이상 활성 권위가 아니다.

운영 상태 문서는 현재 상태만 유지한다. 커밋별 작업 일지를 누적하거나 오래된 상태를
그대로 남기지 않는다.

## 작업 재개와 인수인계

작업을 재개하기 전에 다음 절차를 수행한다.

1. 이 파일과 `PLAN.md`, `HANDOFF.md`, `DECISIONS.md`, `KNOWN_ISSUES.md`, 적용 범위 내의 더
   구체적인 `AGENTS.md`를 읽는다.
2. 브랜치, HEAD, upstream, worktree, staged/unstaged 변경 및 진행 중인 merge, rebase,
   cherry-pick을 확인한다.
3. 인수인계에 기록된 파일과 코드 위치가 실제로 존재하는지 확인한다.
4. 다음 작업의 전제가 되는 최근 검증 결과는 가장 작은 관련 명령으로 다시 확인한다.
5. 인수인계와 실제 저장소가 다르면 실제 상태를 우선하고 차이를 기록한다.
6. 검증된 첫 미완료 단계부터 계속하며 완료된 구현을 반복하지 않는다.

의미 있는 체크포인트 또는 세션 이전 전에 `HANDOFF.md`를 갱신한다. 목표, Git 상태,
변경 파일, 정상·실패 동작, 검증 명령과 결과, 알려진 차단 요인, 구체적인 다음 한 단계를
기록한다. 비밀정보의 실제 값은 절대 기록하지 않는다.

컨텍스트가 길다는 이유만으로 세션을 이전하지 않는다. 명령, 잠금, 서버, 임시 프로세스가
남아 있지 않고 저장소만으로 복원 가능한 체크포인트에서만 이전한다.

## 범위와 구현 원칙

- `main`에서 직접 작업하며 사용자가 명시적으로 요청하지 않으면 브랜치를 만들지 않는다.
- 기존 사용자 변경을 보존하고 관련 없는 수정, rename-only 변경, 대규모 문서 재배치를
  피한다.
- 한 변경은 하나의 Trust Boundary 또는 Vertical Slice에 집중한다.
- 명시적인 마이그레이션·롤백 계약 없이 기존 public import, CLI 입력, artifact reader,
  wire format을 깨지 않는다.
- Discovery와 Planning은 Campaign Scope를 확장하거나 발견된 인터페이스를 실행 권한으로
  전환할 수 없다.
- 사용자가 명시적으로 요청하지 않으면 서브에이전트나 병렬 에이전트를 만들지 않는다.

## Git과 외부 상태

- 사용자의 명시적 승인 없이 commit, push, merge, 원격 브랜치 생성·삭제, publish,
  deploy를 수행하지 않는다.
- 승인된 커밋은 `<type>(<scope>): <한글 제목>` 형식의 한국어 Conventional Commit을 쓴다.
- 비밀정보, 토큰, 자격증명, 인증서, `.env`, 개인 로컬 설정을 커밋하지 않는다.
- SSL 검증을 유지한다. Git OpenSSL CA가 실패하면
  `git -c http.sslBackend=schannel push origin main`을 사용하고 원격 검증에도 같은 설정을
  적용한다.
- 커밋 전에 staged diff를 재검토하고, 승인된 push 뒤에는 local HEAD, upstream, 실제 원격,
  clean worktree를 확인한다.

## 문서화

`docs/DOCUMENTATION_POLICY.md`를 따른다.

- 루트 운영 상태 문서 5개는 단일 한글본으로 유지한다.
- README, RFC, ADR, 버전형 계약과 나머지 저장소 Markdown은 단일 영어본으로 유지한다.
- 모든 tracked 문서는 향후 공개될 수 있는 자료로 취급한다. 절대 로컬 체크아웃 경로, 개인 식별자,
  endpoint-security 제품·process·driver·socket 이름, account·billing 상태와 저장소 밖 backup 이름은
  기록하지 않는다. 환경 제약은 재현 가능한 영향과 public-safe 검증 경로만 일반화해 남긴다.
- 코드와 함께 바뀌어야 하는 계약·보안 경계 문서는 같은 변경에 포함한다.
- 지속적인 아키텍처 근거는 새 ADR에 기록하며, 채택된 ADR은 append-only로 다룬다.
- `DECISIONS.md`에 ADR 본문을 복제하거나 `PLAN.md`·`HANDOFF.md`에 상세 구현 이력을
  누적하지 않는다.
- 운영 상태 문서는 그 상태를 만든 체크포인트와 함께 갱신한다.

## 검증과 실패 처리

코드 변경은 위험에 비례해 검증한다. 일반적으로 집중 pytest, Ruff 전체, Linux 대상 strict
mypy, 가능한 범위의 전체 pytest를 실행한다. 정확한 결과를 기록하고 코드 회귀와 환경
제약을 `KNOWN_ISSUES.md`와 `HANDOFF.md`에서 구분한다.

명령이 실패하면 출력을 확인하고 코드·테스트·환경·외부 상태 문제로 분류한다. 최소 재현
조건을 찾고 같은 실패를 근거 없이 반복하지 않는다. 실행하지 않은 검증을 통과했다고
보고하지 않는다.

## 보안과 파괴적 작업

명시적 승인 없이 비밀정보 노출, 운영 환경 변경, 데이터 삭제, 비가역적 마이그레이션,
강제 push, 원격 브랜치 삭제, 승인 우회, 비용 발생 외부 자원 생성을 수행하지 않는다.
파괴적 작업 전에 정확한 대상을 확인하고 가능하면 복구 가능한 방법을 사용한다.
