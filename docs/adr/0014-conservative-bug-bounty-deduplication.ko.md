> Languages: [English](0014-conservative-bug-bounty-deduplication.en.md) | [한국어](0014-conservative-bug-bounty-deduplication.ko.md)

# ADR 0014: 보수적인 Bug Bounty Finding 중복 제거

- 상태: 중복 제거에 대해 승인됨. 제출 자격은 ADR 0027에서 개정됨
- 날짜: 2026-07-13
- 확인 의미 체계 개정: [ADR 0027](0027-independent-reproduction-confirmation-boundary.ko.md)

> 현재 리포터는 레거시 검증 Finding을 사용할 수 있다. 이들은 재현으로 뒷받침된 Confirmed
> 제출물이 아니라 로컬 검토 초안이다. ADR 0027 마이그레이션 이후에는 Candidate에 결합된
> ReplayOutcome이 성공한 Finding만 제출 준비 상태가 될 수 있다.

## 배경

Bug Bounty 프로그램은 중복 제출에 불이익을 주지만, 공격적인 의미 기반 규칙이나 LLM 유사도
규칙은 서로 다른 영향받는 엔드포인트, 권한 부여 경계, 영향 또는 회귀를 숨길 수 있다. 보고서
생성기는 오래되거나 변조된 Campaign의 Finding이 현재 검토한 프로그램 정책에 따라 생성된
것처럼 제시되는 것도 방지해야 한다.

PAJIN의 일반 `Finding`은 이미 검증, 대상, 재현 및 증거를 기록한다. Bug Bounty 제출과 보수적
중복 제거에는 입증된 영향, 영향받는 구성 요소, 근본 원인 및 해결 방안이 추가로 필요하다.
다른 Mode Pack과 이전 아티팩트에서는 이러한 필드를 선택 사항으로 유지하되, Bug Bounty 데이터가
누락된 사실은 드러나야 한다.

## 결정

`Finding`에 선택적 `impact`, `affected_component`, `root_cause`, `remediation` 필드를 추가한다.
Bug Bounty 리포터는 완료된 Run만 로드하고 트리아지 전에 다음 사항을 검사한다.

1. Run의 Campaign이 `bug-bounty`이고 선택한 프로그램에 속한다.
2. 권한 부여 증거에 현재 정규 프로그램 범위 다이제스트가 포함되어 있다.
3. 컴파일된 대상, 허용/거부 범위, 위험, 메서드, Tool 범주, 금지 사항, 중지 조건, 속도,
   테스트 시간대 및 예산이 검토한 프로그램과 여전히 일치한다.
4. 각 Finding이 현재 검증 호환성 게이트를 통과하고 선언된 허용 엔드포인트를 대상으로 한다.
   ADR 0027 마이그레이션 이후 제출 준비 상태가 되려면 Candidate에 결합된 ReplayOutcome의
   성공도 필요하다.
5. 모든 증거 경로가 동일한 Run의 `evidence/` 디렉터리 아래에 있는 실제 파일로 해석된다.

선택적 `BugBountyFindingIndex`는 알려진 외부 Finding을 프로그램에 엄격히 결합한 스냅샷이다.
실행 권한을 부여하지 않는다.

### 정확한 지문

정확한 SHA-256 지문에는 다음 항목이 포함된다.

- 프로그램 이름
- 쿼리 값을 제외한 정규화된 스킴, 권한부, 경로 및 정렬된 쿼리 매개변수 이름
- 정규화된 취약점 클래스
- 정규화된 영향받는 구성 요소
- 정규화된 근본 원인

민감한 토큰과 우발적인 테스트 값이 ID에 결합되는 일을 피하기 위해 쿼리 값은 제외한다. 구성
요소나 근본 원인이 없는 Finding에는 해당 Finding에만 적용되는 중복 제거 불가 지문을 부여하고
`needs-review`로 표시한다.

알려진 Finding 중 `open`, `triaged`, `accepted` 또는 이미 `duplicate` 상태인 항목과 정확히
일치하면 `known-duplicate`다. 하나의 Run에서 두 번째로 정확히 일치하는 항목은
`run-duplicate`이며, 신뢰도가 더 높은 항목을 기본 항목으로 유지한다. 자동으로 억제하는 경우는
이 두 가지뿐이다.

### 원인 지문

두 번째 지문은 대상 경로를 권한부로 대체한다. 이를 통해 여러 엔드포인트에 걸쳐 동일한 클래스,
구성 요소 및 근본 원인을 식별한다. 일치해도 중복 후보일 뿐이다. 해당 클러스터에서 정확한
지문이 서로 다른 모든 항목은 `needs-review`가 되며 제출 초안은 계속 제공한다.

알려진 `resolved` Finding과 정확히 일치하는 경우도 회귀일 수 있으므로 `needs-review`가 된다.
프로그램에서 요구하는 보고 필드가 누락된 경우에도 동일하게 처리한다. 유사한 제목, 임베딩 또는
LLM 판단으로 Finding을 억제하지 않는다.

## 아티팩트

고유한 각 트리아지 입력에는 콘텐츠에서 파생한 `triage_<digest>` 디렉터리를 부여한다. 이
디렉터리에는 타입이 지정된 JSON 레코드, Markdown 요약 및 `ready` 또는 `needs-review`인 각
Finding의 제출 초안 하나가 포함된다. 정확한 중복에는 초안을 만들지 않는다. 동일한 입력을 다시
실행하면 첫 번째 보고서 세트를 덮어쓰는 대신 실패한다. 완료 이벤트에는 아티팩트 경로와 집계
개수만 기록한다.

Markdown 콘텐츠에는 HTML 및 Markdown 이스케이프를 적용한다. 출력은 초안으로 남으며 이
워크플로를 통해 외부에 제출되지 않는다.

## 결과 및 한계

규칙은 재현 가능하고 설명할 수 있으며 서로 다른 취약점을 숨기지 않는 쪽으로 편향되어 있다.
의도적으로 의미 기반 중복 제거기보다 운영자에게 더 많은 작업을 남길 수 있다.

알려진 Finding 인덱스는 현재 로컬 스냅샷이며 HackerOne, Bugcrowd, Jira 또는 GitHub와 동기화되지
않는다. 로컬 Run 아티팩트는 아직 영속 증거 서비스가 서명하지 않으므로 동일 Run 내 경로 검증은
대체 경계를 탐지하지만 권한이 있는 파일 시스템 변조는 탐지하지 못한다. 리포터는 CVSS를
계산하거나 비즈니스 영향을 입증하거나 보고서를 제출하지 않는다. 레거시 검증에서 유래한 초안
또한 제품 수준 확인을 입증하지 못한다. 이러한 기능은 향후 검증된 통합으로 남는다.

## 검증

테스트는 정확히 일치하는 동일 Run 내 억제, 미해결 및 해결된 알려진 항목과의 일치, 동일 원인의
다중 엔드포인트 검토, 필드가 불완전한 TODO 초안, HTML 이스케이프, 오래된 다이제스트 거부,
컴파일된 정책 변조, 증거 누락, 타입 지정 인덱스 로딩, 콘텐츠에서 파생한 보고서 ID, 반복 쓰기
방지, 이벤트 방출 및 CLI 아티팩트 생성을 다룬다.
